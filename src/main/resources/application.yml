spring:
  profiles.active: default

---
spring:
  profiles: default
  jmx.enabled: false
  
example:
  scheduledJob: 
    enabled: true
  incoming:
    comments:
      dir: /tmp/comments

network:
  path:
    in: /tmp/comments/
    out: /tmp/out
  poolCount: 5
